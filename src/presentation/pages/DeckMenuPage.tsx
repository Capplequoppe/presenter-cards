/**
 * Deck menu page — main deck management surface.
 * Fully implemented in Task 5.2; this shell satisfies the routing contract.
 */
export function DeckMenuPage() {
	return (
		<div className="min-h-screen bg-[#121212] text-gray-100 flex items-center justify-center">
			<h1 className="text-3xl font-bold tracking-tight">Presenter Cards</h1>
		</div>
	);
}
