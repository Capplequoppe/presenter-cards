/**
 * Placeholder for the presenter screen implemented in Phase 6.
 * Renders a minimal dark screen so the route is functional.
 */
export function PresenterPlaceholder() {
	return (
		<div className="min-h-screen bg-gray-950 text-gray-100 flex items-center justify-center">
			<p className="text-gray-400">Presenter screen — coming in Phase 6</p>
		</div>
	);
}
